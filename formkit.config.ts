import { fr } from '@formkit/i18n';
import { defaultConfig } from '@formkit/vue';
// import { rootClasses } from 'formkit.theme';
// import { rootClasses } from "@formkit/themes";

export default defaultConfig({
  locales: { },
  config:{
    // rootClasses,
  }
  
})